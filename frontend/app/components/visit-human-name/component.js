import Ember from 'ember';

export default Ember.Component.extend({
  tagName: 'span'
  // short_date: function() {
  //   return moment(this.get('visit.date')).format('DD.MM h:mm');
  // }.property('visit')
});
