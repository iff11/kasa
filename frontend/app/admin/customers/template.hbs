<h2>
  <div class="row">
    <div class="col-xs-6">
      {{t 'customers.all'}}
    </div>

    <div class="col-xs-6 text-right">
      <button class="btn btn-success" {{action 'createCustomer'}}>
        {{fa-icon 'plus-square'}} {{t 'customer.add'}}
      </button>
    </div>
  </div>
</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th class="xol-xs-1">{{t 'customer.visits_length'}}</th>
      <th class="col-xs-2">{{t 'customer.first_name'}}</th>
      <th class="col-xs-2">{{t 'customer.last_name'}}</th>
      <th class="col-xs-2">{{t 'customer.birth'}}</th>
      <th class="col-xs-1">{{t 'customer.phone'}}</th>
      <th class="col-xs-2">{{t 'customer.mail'}}</th>
      <th class="col-xs-1">{{t 'customer.note'}}</th>
      <th class="col-xs-1 text-right">{{t 'customer.action'}}</th>
    </tr>
  </thead>

  <tbody>
    {{#each customer in attrs.customers}}
      <tr>
        <td>{{customer.visits_count}}</td>
        <td>{{x-visit/last-visit visit=customer.last_visit}}</td>
        <td>{{customer.first_name}}</td>
        <td>{{customer.last_name}}</td>
        <td>{{customer.birth}}</td>
        <td>{{customer.phone}}</td>
        <td>{{customer.mail}}</td>
        <td>{{customer.note}}</td>
        <td class="text-right">
          {{#link-to 'admin.customer' customer class='btn btn-default btn-xs'}}
            {{fa-icon 'pencil'}}
          {{/link-to}}
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
