<h2>
  <div class="row">
    <div class="col-xs-6">
      {{t 'customers.all'}}
    </div>

    <div class="col-xs-6 text-right">
      <button class="btn btn-success" {{action 'createCustomer'}}>
        {{fa-icon 'plus-square'}} {{t 'customer.add'}}
      </button>
    </div>
  </div>
</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th class="xol-xs-1">{{th-sort action="sortBy" title=(t 'customer.visits_length') current=sortBy value='visits_count'}}</th>
      <th class="xol-xs-1">{{th-sort action="sortBy" title=(t 'customer.lastVisit') current=sortBy value='last_visit.date'}}</th>
      <th class="col-xs-2">{{th-sort action="sortBy" title=(t 'customer.first_name') current=sortBy value='first_name'}}</th>
      <th class="col-xs-2">{{th-sort action="sortBy" title=(t 'customer.last_name') current=sortBy value='last_name'}}</th>
      <th class="col-xs-2">{{th-sort action="sortBy" title=(t 'customer.birth') current=sortBy value='birth'}}</th>
      <th class="col-xs-1">{{th-sort action="sortBy" title=(t 'customer.phone') current=sortBy value='phone'}}</th>
      <th class="col-xs-2">{{th-sort action="sortBy" title=(t 'customer.mail') current=sortBy value='mail'}}</th>
      <th class="col-xs-1">{{th-sort action="sortBy" title=(t 'customer.note') current=sortBy value='note'}}</th>
      <th class="col-xs-1 text-right">{{t 'customer.action'}}</th>
    </tr>
  </thead>

  <tbody>
    {{#each customersSorted as |customer|}}
      <tr>
        <td>{{customer.visits_count}}</td>
        <td>{{x-visit/last-visit visit=customer.last_visit}}</td>
        <td>{{customer.first_name}}</td>
        <td>{{customer.last_name}}</td>
        <td>{{customer.birth}}</td>
        <td>{{customer.phone}}</td>
        <td>{{customer.mail}}</td>
        <td>{{customer.note}}</td>
        <td class="text-right">
          {{#link-to 'admin.customer' customer class='btn btn-default btn-xs'}}
            {{fa-icon 'pencil'}}
          {{/link-to}}
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
