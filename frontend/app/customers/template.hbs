<h2>{{t 'customers.all'}}</h2>

<table class='table'>
  <thead>
    <tr>
      <th class="col-xs-2">{{t 'customer.full_name'}}</th>
      <th class="col-xs-2">{{t 'customer.birth'}}</th>
      <th class="col-xs-2 text-right">{{t 'customer.birthday_in'}}</th>
      <th class="col-xs-6">{{t 'customer.note'}}</th>
    </tr>
  </thead>
  <tbody>
    {{#each customer in attrs.customers}}
      <tr>
        <td>
          {{customer.first_name}} {{customer.last_name}}
        </td>
        <td>
          {{customer.birth}}
        </td>
        <td class="text-right">
          {{x-customer/birthday customer=customer}}
        </td>
        <td>
          {{customer.note}}
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
