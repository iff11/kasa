export default function(/*server*/) {
  // server.createList('employee', 3);

  // Seed your development database using your factories. This
  // data will not be loaded in your tests.

  // server.createList('contact', 10);
}
